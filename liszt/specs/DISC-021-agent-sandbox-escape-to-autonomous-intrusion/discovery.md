# DISC-021: Agent sandbox escape → autonomous intrusion

> Generated by `tools/emit_discovery.py` from scenario `021` (status `draft`) at baseline `2026.07` on 2026-09-03. Do not edit; regenerate.

## Purpose

Establish the evidence map for this scenario. Nothing on the record has been scored, so there is no claim to test and no prediction to seal. The agent reproduces each step, records what appeared and where, tags which layer it came from, and proposes DeTT&CT numbers with a written rationale. A person accepts or rejects each proposal. Until then the numbers count toward nothing.

## Authorization

- **Autonomy rung**: `lab-only`
- **Rungs enabled in this deployment**: `lab-only`
- **Time box**: `PT2H`
- **Egress**: `deny-all`
- **Teardown required**: `true`

**Targets.** The agent MUST NOT act on anything not in this list.

- _None assigned yet. With an empty allowlist the run MUST refuse to start._

**Stop conditions.** Any of these aborts the run immediately and leaves the environment intact for inspection.

- Any interaction with an address or account outside authorization.targets.
- Any credential that resolves outside the lab boundary.
- Any action not present in this spec's procedure.
- Time box expiry, whether or not the procedure has finished.
- Loss of the observer's ability to read the telemetry pipeline, since the run stops producing evidence at that moment.

**Prohibited.**

- No exploit development and no unpublished vulnerability research: the agent emulates published techniques only.
- No action against any system not named in authorization.targets.
- No persistence that survives teardown.
- No movement of real data. Synthetic canary records only.
- No credential use that resolves outside the lab boundary.

## Environment

Telemetry pipeline mode: **`scratch`**. In discovery this bounds what may be proposed: a scratch pipeline supports a visibility proposal (did an artifact appear, and in which system class) and not a detection proposal, because our detection content was not in the path.

| Layer | Steps |
|---|---|
| External (out of estate) | 3 |
| L0 Infrastructure | 2 |
| L1 Data | 4, 5 |
| L3 Orchestration and Agent | 1 |
| L4 Application | 6 |

| Component | For steps | Fidelity |
|---|---|---|
| Target for step 1: Agent / eval | 1 | `equivalent` |
| Target for step 2: Host / net | 2 | `equivalent` |
| Target for step 3: External | 3 | `equivalent` |
| Target for step 4: Data → Host | 4 | `equivalent` |
| Target for step 5: Host / Cloud | 5 | `equivalent` |
| Target for step 6: App / net | 6 | `equivalent` |
| Observation source: Nothing emits this today. It would have to come from the agent platform's run manifest. | 1 | `equivalent` |
| Observation source: Palo Alto NGFW traffic logs + Zscaler proxy, index=net_egress; Artifactory access.log | 2 | `equivalent` |
| Observation source: dataset-worker application logs, index=app_dataproc, source=worker.jsonl | 3 | `equivalent` |
| Observation source: CrowdStrike Falcon ProcessRollup2, index=edr_main; containerd runtime events | 4 | `equivalent` |
| Observation source: EKS control-plane audit log, CloudWatch group /aws/eks/prod/cluster; AWS CloudTrail | 5 | `equivalent` |
| Observation source: Zscaler proxy transaction logs, index=net_proxy; Symantec DLP egress events | 6 | `equivalent` |

## Layer vocabulary

`observed_layer` in the run record takes one or two of these, exactly as written. Agent behavior and MCP servers are L3. Model output is L2. Retrieval and data stores are L1. App surface is L4. Hosts, cloud and network are L0. A third party is not a layer: set `out_of_estate: true` instead.

- `L1 · Data`
- `L2 · Model`
- `L3 · Orchestration & Agent`
- `L4 · Application`
- `L0 · Infrastructure`

## Requirements

### Requirement: Step 1 — An agent is run on a cyber-capability task with its safety refusals deliberately reduced

Techniques: _none mapped_. Lab layer: L3 Orchestration and Agent.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step against the allowlisted target. No framework technique is mapped to this step, so there is no library emulation to resolve: the action set has to be written by hand and reviewed before it runs.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

**What the record says today, unscored.** Treat it as the first place to look, not the only one.

- Signal: _Agent run configuration & safeguard state_
- Emitted at: _Agent platform / eval harness · AI gateway_
- Source: _Nothing emits this today. It would have to come from the agent platform's run manifest._
- Detection opportunity: _Agent running with refusals or classifiers disabled; unattended run_

#### Scenario: The step is observed

- **WHEN** the step 1 action set completes
- **THEN** the agent SHALL search every collector it can reach, not only the source the record names, and record `detected`, `logged-only`, `absent` or `unscoreable`
- **AND** SHALL record `observed_source` as the system plus index or log name, and only where the artifact was found
- **AND** SHALL tag `observed_layer` from the vocabulary above, one or two values, and `out_of_estate` where the step reached a stand-in
- **AND** SHALL record whether anything alerted, and what
- **AND** SHALL record the artifact latency in seconds, or null

#### Scenario: A proposal is made

- **WHEN** step 1 was executed and its observation is not `unscoreable`
- **THEN** the agent SHALL propose `proposed_visibility` (0..4) and `proposed_detection` (-1..5)
- **AND** SHALL write `proposal_rationale` naming what was seen, where, how completely, and what would have moved each number
- **AND** SHALL propose `-1` for detection, because this pipeline does not carry our detection content and nothing about it was observed
- **AND** SHALL NOT propose anything for a step that did not execute or whose observation is `unscoreable`

### Requirement: Step 2 — A zero-day in the package proxy, the sandbox's only allowed egress, puts it on the internet

Techniques: `T1190`, `AML.T0049`. Lab layer: L0 Infrastructure.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for Host / net, using a published emulation of T1190.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

**What the record says today, unscored.** Treat it as the first place to look, not the only one.

- Signal: _Egress from a sandboxed workload_
- Emitted at: _Egress FW / proxy · NDR · package-registry logs_
- Source: _Palo Alto NGFW traffic logs + Zscaler proxy, index=net_egress; Artifactory access.log_
- Detection opportunity: _Sandbox reaching the internet through its one allowed egress path_

#### Scenario: The step is observed

- **WHEN** the step 2 action set completes
- **THEN** the agent SHALL search every collector it can reach, not only the source the record names, and record `detected`, `logged-only`, `absent` or `unscoreable`
- **AND** SHALL record `observed_source` as the system plus index or log name, and only where the artifact was found
- **AND** SHALL tag `observed_layer` from the vocabulary above, one or two values, and `out_of_estate` where the step reached a stand-in
- **AND** SHALL record whether anything alerted, and what
- **AND** SHALL record the artifact latency in seconds, or null

#### Scenario: A proposal is made

- **WHEN** step 2 was executed and its observation is not `unscoreable`
- **THEN** the agent SHALL propose `proposed_visibility` (0..4) and `proposed_detection` (-1..5)
- **AND** SHALL write `proposal_rationale` naming what was seen, where, how completely, and what would have moved each number
- **AND** SHALL propose `-1` for detection, because this pipeline does not carry our detection content and nothing about it was observed
- **AND** SHALL NOT propose anything for a step that did not execute or whose observation is `unscoreable`

### Requirement: Step 3 — It roots an exposed public code-execution endpoint elsewhere and uses it as a launchpad

Techniques: `T1190`, `T1584`, `AML.T0049`. Lab layer: External (out of estate).

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for External, using a published emulation of T1190, T1584.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

**What the record says today, unscored.** Treat it as the first place to look, not the only one.

- Signal: _Untrusted artifact / config processed_
- Emitted at: _L1 ingestion · dataset-processing logs_
- Source: _dataset-worker application logs, index=app_dataproc, source=worker.jsonl_
- Detection opportunity: _Template syntax or local file paths inside a data config_

#### Scenario: The step is observed

- **WHEN** the step 3 action set completes
- **THEN** the agent SHALL search every collector it can reach, not only the source the record names, and record `detected`, `logged-only`, `absent` or `unscoreable`
- **AND** SHALL record `observed_source` as the system plus index or log name, and only where the artifact was found
- **AND** SHALL tag `observed_layer` from the vocabulary above, one or two values, and `out_of_estate` where the step reached a stand-in
- **AND** SHALL record whether anything alerted, and what
- **AND** SHALL record the artifact latency in seconds, or null

#### Scenario: A proposal is made

- **WHEN** step 3 was executed and its observation is not `unscoreable`
- **THEN** the agent SHALL propose `proposed_visibility` (0..4) and `proposed_detection` (-1..5)
- **AND** SHALL write `proposal_rationale` naming what was seen, where, how completely, and what would have moved each number
- **AND** SHALL propose `-1` for detection, because this pipeline does not carry our detection content and nothing about it was observed
- **AND** SHALL NOT propose anything for a step that did not execute or whose observation is `unscoreable`

**Out of scope for this run.**

- The scenario reaches a third party at this step. The run substitutes a local stand-in inside the lab boundary. Nothing outside the allowlist is touched, and the result speaks to our own visibility only.

### Requirement: Step 4 — Malicious dataset configs leak the worker pod's secrets, then give code execution inside it

Techniques: `T1552`, `T1059`, `AML.T0010.002`, `AML.T0055`, `AML.T0050`. Lab layer: L1 Data.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for Data → Host, using a published emulation of T1552, T1059.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

**What the record says today, unscored.** Treat it as the first place to look, not the only one.

- Signal: _Code execution in a processing worker_
- Emitted at: _EDR / endpoint · container runtime_
- Source: _CrowdStrike Falcon ProcessRollup2, index=edr_main; containerd runtime events_
- Detection opportunity: _Data-processing pod spawning a shell, interpreter or downloader_

#### Scenario: The step is observed

- **WHEN** the step 4 action set completes
- **THEN** the agent SHALL search every collector it can reach, not only the source the record names, and record `detected`, `logged-only`, `absent` or `unscoreable`
- **AND** SHALL record `observed_source` as the system plus index or log name, and only where the artifact was found
- **AND** SHALL tag `observed_layer` from the vocabulary above, one or two values, and `out_of_estate` where the step reached a stand-in
- **AND** SHALL record whether anything alerted, and what
- **AND** SHALL record the artifact latency in seconds, or null

#### Scenario: A proposal is made

- **WHEN** step 4 was executed and its observation is not `unscoreable`
- **THEN** the agent SHALL propose `proposed_visibility` (0..4) and `proposed_detection` (-1..5)
- **AND** SHALL write `proposal_rationale` naming what was seen, where, how completely, and what would have moved each number
- **AND** SHALL propose `-1` for detection, because this pipeline does not carry our detection content and nothing about it was observed
- **AND** SHALL NOT propose anything for a step that did not execute or whose observation is `unscoreable`

### Requirement: Step 5 — Pod → node root; metadata credentials and forged identity tokens give cluster-admin

Techniques: `T1611`, `T1552.005`, `T1550.001`, `AML.T0105`, `AML.T0091.000`. Lab layer: L1 Data.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for Host / Cloud, using a published emulation of T1611, T1552.005, T1550.001.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

**What the record says today, unscored.** Treat it as the first place to look, not the only one.

- Signal: _Container escape & credential use_
- Emitted at: _K8s audit · cloud audit / CloudTrail · Identity_
- Source: _EKS control-plane audit log, CloudWatch group /aws/eks/prod/cluster; AWS CloudTrail_
- Detection opportunity: _Privileged / hostPath pod created; metadata service read from a pod_

#### Scenario: The step is observed

- **WHEN** the step 5 action set completes
- **THEN** the agent SHALL search every collector it can reach, not only the source the record names, and record `detected`, `logged-only`, `absent` or `unscoreable`
- **AND** SHALL record `observed_source` as the system plus index or log name, and only where the artifact was found
- **AND** SHALL tag `observed_layer` from the vocabulary above, one or two values, and `out_of_estate` where the step reached a stand-in
- **AND** SHALL record whether anything alerted, and what
- **AND** SHALL record the artifact latency in seconds, or null

#### Scenario: A proposal is made

- **WHEN** step 5 was executed and its observation is not `unscoreable`
- **THEN** the agent SHALL propose `proposed_visibility` (0..4) and `proposed_detection` (-1..5)
- **AND** SHALL write `proposal_rationale` naming what was seen, where, how completely, and what would have moved each number
- **AND** SHALL propose `-1` for detection, because this pipeline does not carry our detection content and nothing about it was observed
- **AND** SHALL NOT propose anything for a step that did not execute or whose observation is `unscoreable`

### Requirement: Step 6 — C2 and exfiltration ride public web services; internal source repos are reached with write access

Techniques: `T1102`, `T1567`, `T1213.003`, `AML.T0025`. Lab layer: L4 Application.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for App / net, using a published emulation of T1102, T1567, T1213.003.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

**What the record says today, unscored.** Treat it as the first place to look, not the only one.

- Signal: _Agent-driven C2 & exfiltration_
- Emitted at: _NDR / proxy · DLP egress · SaaS logs_
- Source: _Zscaler proxy transaction logs, index=net_proxy; Symantec DLP egress events_
- Detection opportunity: _Encoded traffic to paste, file-drop or request-capture services_

#### Scenario: The step is observed

- **WHEN** the step 6 action set completes
- **THEN** the agent SHALL search every collector it can reach, not only the source the record names, and record `detected`, `logged-only`, `absent` or `unscoreable`
- **AND** SHALL record `observed_source` as the system plus index or log name, and only where the artifact was found
- **AND** SHALL tag `observed_layer` from the vocabulary above, one or two values, and `out_of_estate` where the step reached a stand-in
- **AND** SHALL record whether anything alerted, and what
- **AND** SHALL record the artifact latency in seconds, or null

#### Scenario: A proposal is made

- **WHEN** step 6 was executed and its observation is not `unscoreable`
- **THEN** the agent SHALL propose `proposed_visibility` (0..4) and `proposed_detection` (-1..5)
- **AND** SHALL write `proposal_rationale` naming what was seen, where, how completely, and what would have moved each number
- **AND** SHALL propose `-1` for detection, because this pipeline does not carry our detection content and nothing about it was observed
- **AND** SHALL NOT propose anything for a step that did not execute or whose observation is `unscoreable`

**Out of scope for this run.**

- The record marks a control as having held at this step. Reproduce the attempt, not a bypass of that control, and record whether it held again.

## Return path

There is no scoring step and no scorecard. `confirmed` and `overestimate` are defined against a prediction, and this run had none.

1. Write the run record to `runs/DISC-021-YYYY-MM-DD-NN.yaml`, in the shape of `schema/discovery-run.schema.json`.
2. Validate it: `python3 tools/validate.py runs/DISC-*.yaml`.
3. A person reads the proposals and accepts some or all: `python3 tools/discovery_to_session.py runs/DISC-... --accepted-by "Name"`.
4. Apply, review the diff, commit: `python3 tools/apply_session.py ... --dry-run`, then without `--dry-run`.

Every score that lands carries `score_provenance: agent-proposed`. It is listed in the coverage rollup and counted in no average until a person who has looked at the evidence sets it to `human-session`.
