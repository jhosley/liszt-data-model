# ST-021: Agent sandbox escape → autonomous intrusion

> Generated by `tools/emit_testspec.py` from scenario `021` at baseline `2026.07` on 2026-09-11. Do not edit; regenerate.

## Purpose

Test whether the evidence map for this scenario is right. The agent reproduces the mapped behavior; the observer checks whether each artifact appeared, in the system the record names, and whether the named detection fired. The claims under test are sealed in `prediction.yaml` and must not be read before the observations are recorded.

## Authorization

- **Autonomy rung**: `lab-only`
- **Rungs enabled in this deployment**: `lab-only`
- **Time box**: `PT2H`
- **Egress**: `deny-all`
- **Teardown required**: `true`

**Targets.** The agent MUST NOT act on anything not in this list.

- _None assigned yet. With an empty allowlist the run MUST refuse to start._

**Stop conditions.** Any of these aborts the run immediately and leaves the environment intact for inspection.

- Any interaction with an address or account outside authorization.targets.
- Any credential that resolves outside the lab boundary.
- Any action not present in this spec's procedure.
- Time box expiry, whether or not the procedure has finished.
- Loss of the observer's ability to read the telemetry pipeline, since the run stops producing evidence at that moment.

**Prohibited.**

- No exploit development and no unpublished vulnerability research: the agent emulates published techniques only.
- No action against any system not named in authorization.targets.
- No persistence that survives teardown.
- No movement of real data. Synthetic canary records only.
- No credential use that resolves outside the lab boundary.

## Environment

Telemetry pipeline mode: **`mirrored`**. This decides which claims may be scored, and it is the difference between a result that transfers to the estate and one that does not.

| Layer | Steps |
|---|---|
| External (out of estate) | 3 |
| L0 Infrastructure | 2 |
| L1 Data | 4, 5 |
| L3 Orchestration and Agent | 1 |
| L4 Application | 6 |

| Component | For steps | Fidelity |
|---|---|---|
| Target for step 1: Agent / eval | 1 | `equivalent` |
| Target for step 2: Host / net | 2 | `equivalent` |
| Target for step 3: External | 3 | `equivalent` |
| Target for step 4: Data → Host | 4 | `equivalent` |
| Target for step 5: Host / Cloud | 5 | `equivalent` |
| Target for step 6: App / net | 6 | `equivalent` |
| Observation source: Nothing emits this today. It would have to come from the agent platform's run manifest. | 1 | `exact` |
| Observation source: Palo Alto NGFW traffic logs + Zscaler proxy, index=net_egress; Artifactory access.log | 2 | `exact` |
| Observation source: dataset-worker application logs, index=app_dataproc, source=worker.jsonl | 3 | `exact` |
| Observation source: CrowdStrike Falcon ProcessRollup2, index=edr_main; containerd runtime events | 4 | `exact` |
| Observation source: EKS control-plane audit log, CloudWatch group /aws/eks/prod/cluster; AWS CloudTrail | 5 | `exact` |
| Observation source: Zscaler proxy transaction logs, index=net_proxy; Symantec DLP egress events | 6 | `exact` |

- Mirrored pipeline: the lab ships to the same collectors, parsers and detection content as the estate being scored. This is the only mode in which a detection claim is genuinely under test, and the only mode whose Blind results transfer.
- Any source named on an evidence row but absent from the lab makes that row's source claim unscoreable. Record it as unscoreable in the run; do not infer it.

## Requirements

### Requirement: Step 1 — An agent is run on a cyber-capability task with its safety refusals deliberately reduced

Techniques: _none mapped_. Layer: L3 Orchestration and Agent.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step against the allowlisted target. No framework technique is mapped to this step, so there is no library emulation to resolve: the action set has to be written by hand and reviewed before it runs.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

#### Scenario: The artifact is produced

- **WHEN** the step 1 action set completes
- **THEN** an artifact matching _Agent run configuration & safeguard state_ SHALL NOT be observable
- **AND** the observer SHALL record the artifact latency in seconds

### Requirement: Step 2 — A zero-day in the package proxy, the sandbox's only allowed egress, puts it on the internet

Techniques: `T1190`, `AML.T0049`. Layer: L0 Infrastructure.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for Host / net, using a published emulation of T1190.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

#### Scenario: The artifact is produced

- **WHEN** the step 2 action set completes
- **THEN** an artifact matching _Egress from a sandboxed workload_ SHALL be observable
- **AND** the observer SHALL record the artifact latency in seconds

#### Scenario: The artifact lands where the record says

- **WHEN** an artifact is observed for step 2
- **THEN** it SHALL be found in `Palo Alto NGFW traffic logs + Zscaler proxy, index=net_egress; Artifactory access.log`
- **AND** if it is found elsewhere the observer SHALL record where, because a right answer from the wrong system is a separate defect

#### Scenario: The detection fires

- **WHEN** the artifact for step 2 reaches the detection pipeline
- **THEN** `SIEM saved search NET-EGRESS-ANOM-07; returns rows for the last 90 days` SHALL fire

### Requirement: Step 3 — It roots an exposed public code-execution endpoint elsewhere and uses it as a launchpad

Techniques: `T1190`, `T1584`, `AML.T0049`. Layer: External (out of estate).

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for External, using a published emulation of T1190, T1584.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

#### Scenario: The artifact is produced

- **WHEN** the step 3 action set completes
- **THEN** an artifact matching _Untrusted artifact / config processed_ SHALL be observable
- **AND** the observer SHALL record the artifact latency in seconds

#### Scenario: The artifact lands where the record says

- **WHEN** an artifact is observed for step 3
- **THEN** it SHALL be found in `dataset-worker application logs, index=app_dataproc, source=worker.jsonl`
- **AND** if it is found elsewhere the observer SHALL record where, because a right answer from the wrong system is a separate defect

**Out of scope for this run.**

- The scenario reaches a third party at this step. The run substitutes a local stand-in inside the lab boundary. Nothing outside the allowlist is touched, and the result speaks to our own visibility only.

### Requirement: Step 4 — Malicious dataset configs leak the worker pod's secrets, then give code execution inside it

Techniques: `T1552`, `T1059`, `AML.T0010.002`, `AML.T0055`, `AML.T0050`. Layer: L1 Data.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for Data → Host, using a published emulation of T1552, T1059.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

#### Scenario: The artifact is produced

- **WHEN** the step 4 action set completes
- **THEN** an artifact matching _Code execution in a processing worker_ SHALL be observable
- **AND** the observer SHALL record the artifact latency in seconds

#### Scenario: The artifact lands where the record says

- **WHEN** an artifact is observed for step 4
- **THEN** it SHALL be found in `CrowdStrike Falcon ProcessRollup2, index=edr_main; containerd runtime events`
- **AND** if it is found elsewhere the observer SHALL record where, because a right answer from the wrong system is a separate defect

#### Scenario: The detection fires

- **WHEN** the artifact for step 4 reaches the detection pipeline
- **THEN** `Detection rule EDR-CONT-0113 (worker process ancestry); 3 true positives in test` SHALL fire

### Requirement: Step 5 — Pod → node root; metadata credentials and forged identity tokens give cluster-admin

Techniques: `T1611`, `T1552.005`, `T1550.001`, `AML.T0105`, `AML.T0091.000`. Layer: L1 Data.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for Host / Cloud, using a published emulation of T1611, T1552.005, T1550.001.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

#### Scenario: The artifact is produced

- **WHEN** the step 5 action set completes
- **THEN** an artifact matching _Container escape & credential use_ SHALL be observable
- **AND** the observer SHALL record the artifact latency in seconds

#### Scenario: The artifact lands where the record says

- **WHEN** an artifact is observed for step 5
- **THEN** it SHALL be found in `EKS control-plane audit log, CloudWatch group /aws/eks/prod/cluster; AWS CloudTrail`
- **AND** if it is found elsewhere the observer SHALL record where, because a right answer from the wrong system is a separate defect

### Requirement: Step 6 — C2 and exfiltration ride public web services; internal source repos are reached with write access

Techniques: `T1102`, `T1567`, `T1213.003`, `AML.T0025`. Layer: L4 Application.

The agent SHALL reproduce this behavior against an allowlisted target, and SHALL NOT take any action outside the action set below.

- Reproduce the behavior described by the step, against the allowlisted target for App / net, using a published emulation of T1102, T1567, T1213.003.
- Resolve the concrete emulation from the adapter's library at run time by technique id. The spec carries no payload by design.
- Record the wall clock time of the action so artifact latency can be measured.

#### Scenario: The artifact is produced

- **WHEN** the step 6 action set completes
- **THEN** an artifact matching _Agent-driven C2 & exfiltration_ SHALL be observable
- **AND** the observer SHALL record the artifact latency in seconds

#### Scenario: The artifact lands where the record says

- **WHEN** an artifact is observed for step 6
- **THEN** it SHALL be found in `Zscaler proxy transaction logs, index=net_proxy; Symantec DLP egress events`
- **AND** if it is found elsewhere the observer SHALL record where, because a right answer from the wrong system is a separate defect

#### Scenario: The detection fires

- **WHEN** the artifact for step 6 reaches the detection pipeline
- **THEN** `Proxy category policy + DLP rule DLP-EGR-22; blocks logged weekly` SHALL fire

**Out of scope for this run.**

- The record marks a control as having held at this step. Reproduce the attempt, not a bypass of that control, and record whether it held again.

## Scoring

After the run, record observations in a run record and score it:

```bash
python3 tools/score_run.py runs/RUN-021-YYYY-MM-DD-01.yaml
```

The scorer refuses to score a claim outside the venue this spec allows, and refuses entirely if the prediction on disk no longer matches the digest the run was bound to.
